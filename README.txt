Luminor Internship Homework

This is a Spring application with built in H2 in-memory DB for payment procesing.

Functionalities:
1. POST /payments endpoint that creates payment resource.
2. POST /payment-files endpoint that acceps CSV and creates payments.
3. GET /payments endpoint that returns list of payments.